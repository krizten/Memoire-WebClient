import React, { FunctionComponent } from 'react';

interface Props {}

export const EntryViewer: FunctionComponent<Props> = () => {
  return (
    <div className="viewer">
      <div className="viewer__controls">Buttons</div>
    </div>
  );
};
